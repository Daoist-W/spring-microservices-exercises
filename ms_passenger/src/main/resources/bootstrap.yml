spring:
  application:
    name: passengerMS
  cloud:
    consul:
      config:
        fail-fast: false
        enabled: true
        prefixes: config
        defaultContext: apps
        data-key: data
        profileSeparator: '::'
        format: YAML