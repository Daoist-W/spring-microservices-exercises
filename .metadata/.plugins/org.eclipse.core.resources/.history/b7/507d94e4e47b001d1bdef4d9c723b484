spring:
  datasource:
    username: demo
    password: demo
    url: jdbc:mysql://localhost:3306/infygo_flight
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update   
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
server:
  servlet.context-path: /api
  port: 9200
  
  
spring:
  application:
    name: flightMS
  cloud:
    consul:
      host: localhost
      discovery:
        hostname: localhost
      config:
        fail-fast: false
        enabled: true
        prefixes: config
        defaultContext: apps
        data-key: data
        profileSeparator: '::'
        format: YAML
